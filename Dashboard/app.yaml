name: Dashboard
language: python
variables:
  - name: ArrivalTrainData
    inputType: InputTopic
    description: Receives the data from trains that are close to their destination
    defaultValue: ArrivalTrainData
    required: true
dockerfile: dockerfile
runEntryPoint: main.py
defaultFile: main.py
